# B5.2 — Account-scoped Core personal profile contract

20 September 2026. This independent Server slice starts from `origin/main`
`bae12c01dd3eb3e9d3a618623fab84f0e8878ef0`. It supplies the Core contract for
future managed personal remote profiles. It does not move device-local profiles
or credentials to Core and does not complete B5.2 or S08.11.

## Acceptance matrix

| Acceptance | Implemented boundary | Evidence |
| --- | --- | --- |
| Home and account isolation | Every route binds the configured Core/home pair and a current bearer principal. The repository always reads and mutates with `(owner_id, profile_id)`; administrator status does not grant access to another account's personal profiles. Foreign profile IDs and missing IDs produce the same 404 response. | Two real accounts create and read independent records, attempt direct cross-account reads, exercise wrong Core/home paths and retain isolation after restart. |
| Optimistic revision and bounded validation | Update and delete require the exact stored revision. A stale operation returns 409 after its savepoint is rolled back. IDs are generated by Core; profiles are capped at 32 per account; label, closed protocol, normalized host, port and username have strict bounds and reject unknown fields. Collection state is authenticated and revisioned. | Stale update/delete, no partial overwrite, capacity, invalid protocol/host/port/text/extra-field and persisted restart tests. |
| Secrets stay outside API, logs and DB metadata | The request contract has no password, token, secret, PIN, key or lease field. Extra fields fail before repository dispatch and static errors never echo their values. Allowed metadata is AES-GCM encrypted with Core/home/account/profile/revision AAD; clear DB columns contain only opaque IDs, revisions, nonce and ciphertext. Audit rows contain action/status/opaque IDs/time only. | Six adversarial forbidden-field requests, captured-log checks, SQLite schema/dump inspection, protected OpenAPI inspection, ciphertext tamper fail-closed and restart validation. |

## API and ownership

`/api/v1/personal-profiles/{core_id}/{home_id}` exposes typed list/create and
`/{profile_id}` exposes typed read/update/delete. All operations use the existing
ready-user dependency, current-session database assertion, request boundary,
static error map and rate limiter. There is no admin collection, grant endpoint,
command endpoint or reusable authorization lease.

Only connection metadata needed to identify a remote target is accepted:
label, SSH/RDP/VNC protocol, normalized host, port and optional username.
Credentials, host trust, private keys, commands, PIN state and Client session
leases remain outside this repository. The Client must still use its local PIN,
route, lifecycle and one-resource lease gates before opening a connection.

## Verification

- Personal profile API/security suite: **11 passed**.
- Related Core context, migration, runtime and boundary regression: **52 passed**.
- New module line coverage: **at least 88%**, exceeding the 80% gate.
- Python compile, execution queue validation, diff check and gitleaks: clean.

Physical targets, Client synchronization, multi-Core discovery and S08.11
central search remain separate acceptance gates, so roadmap counters do not
advance in this commit.
