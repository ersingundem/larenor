package com.ersingundem.larenor.inventory

import androidx.core.content.FileProvider

/** Exposes only explicit, bounded inventory exports from the app cache. */
class InventoryExportFileProvider : FileProvider()
